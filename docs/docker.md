# Docker

- docker bridge
- layers
- MultiStage Docker file
- Networking
- Docker vs VM
- docker compose
- dockerfile
- how containers really works
- network types in docker? (host, bridgr, ...)
- stage in dockerfile
